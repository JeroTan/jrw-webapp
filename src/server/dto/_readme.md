# Server DTO

Transport-facing DTO types and mappers live here when route/controller boundaries need stable API shapes.

API JSON uses camelCase. Map D1 snake_case rows before returning public envelopes.
